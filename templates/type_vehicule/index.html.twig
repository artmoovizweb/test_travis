{% extends 'base.html.twig' %}

{% block title %}TypeVehicule index{% endblock %}

{% block body %}
    <h1 class="mb-5">TypeVehicule index</h1>

    <div class="container">
        <table id="datatable" class="table table-striped table-hover table-responsive cell-border">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Name</th>
                    <th>actions</th>
                </tr>
            </thead>
            <tbody>
            {% for type_vehicule in type_vehicules %}
                <tr>
                    <td>{{ type_vehicule.id }}</td>
                    <td>{{ type_vehicule.name }}</td>
                    <td>
                        <a href="{{ path('type_vehicule_show', {'id': type_vehicule.id}) }}">Show</a>
                        <a> | </a>
                        <a href="{{ path('type_vehicule_edit', {'id': type_vehicule.id}) }}">Edit</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="3">No records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <a href="{{ path('type_vehicule_new') }}">Create new</a>
    </div>
{% endblock %}
